import functions_framework

@functions_framework.http
def handle(request):
    # This function should compare a current image with a previous image
    # if greater than X% trigger the annotation function/
    return request
